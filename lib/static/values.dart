abstract class Values {
  /// Unlocalized application title, used when localization isn't supported.
  /// prefer context.l10n.applicationTitle
  static const applicationTitle = "Numly";
  static const databaseName = "numly_db";

  static const highScoreDecimals = 1;
}
